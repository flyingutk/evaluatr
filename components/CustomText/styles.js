import {
    StyleSheet,
  } from "react-native";
import { Colors } from "../../utils/Constants";

 const styles = StyleSheet.create({
     textStyle :{
         fontSize: 14,
         fontWeight: 'normal',
         color: Colors.black,
     }
})

export default styles