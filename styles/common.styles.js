const spaceBetween = {
    // justifyContent: "space-between",
  };
  
  const spaceInFront = {
    justifyContent: "flex-end",
  };
  
  const spaceInEnd = {
    justifyContent: "flex-start",
  };
  
  const fitCotent = {
    alignItems: "baseline",
  };
  // Directions
  const flexDirectionRow = {
    flexDirection: "row",
  };
  
  const flexOne = {
    flex: 1
  }
  
  const boxRadius = {
  }
  
  
  
  export { spaceBetween, fitCotent, flexDirectionRow, spaceInFront, spaceInEnd,flexOne };
  